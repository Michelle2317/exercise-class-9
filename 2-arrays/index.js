/**
 * Questions 1
 * Create an array with the different fruit types orange, apple, banana. Initialise it to the variable fruits. 
 * Console log out fruits.
 * Write a single line comment as to what will be displayed
 */
var fruits = ["orange", "apple", "banana"];
console.log(fruits) //[ 'orange', 'apple', 'banana' ]

/**
 * Example 2
 * Create an array with the different pet types dog, spider, bird. 
 * Then remove the last item in the array using pop. 
 * Console log out pets.
 * Write a single line comment as to what will be displayed
 */
var pets = ["dog", "spider", "bird"];
pets.pop();
console.log(pets) //[ 'dog', 'spider' ]